FAKE DIFF — never apply this. Fixture for the ux agent.
Two problems are planted in it. Passing means finding both.

--- a/index.html
+++ b/index.html
@@ -229,7 +229,7 @@
 :root {
   --bg: #111;
-  --acid: #d4f500;
+  --acid: #7c5cff;
   --ink: #f5f5f5;
 }

@@ -2539,10 +2539,22 @@
 function renderCard(item) {
   return `
   <article class="card">
     <div class="card-title">${escHtml(item.title)}</div>
+    ${isUnseen(item) ? '<span class="card-new-dot" aria-label="new"></span>' : ''}
     <div class="card-meta">${escHtml(item.source)}</div>
   </article>`
 }

+.card-new-dot {
+  position: absolute;
+  top: 10px;
+  right: 10px;
+  width: 8px;
+  height: 8px;
+  border-radius: 50%;
+  background: var(--acid);
+}
+
